# Copy to /srv/navon/.env on the GPU box and fill in real values.
# Generate AUTH_SECRET with: openssl rand -hex 32
# Generate POSTGRES_PASSWORD with: openssl rand -hex 16

POSTGRES_PASSWORD=changeme
AUTH_SECRET=changeme32charsminimumrandomstring
AUTH_URL=https://portal.navonworld.com
EMAIL_FROM=noreply@navonworld.com
AUTH_RESEND_KEY=dummy
